<Window x:Class="Studio_Rent_Service.Views.Studios.StudioListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ViewModels="clr-namespace:Studio_Rent_Service.ViewModels"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Управление студиями" 
        Height="600" 
        Width="900"
        MinHeight="400" 
        MinWidth="700"
        WindowStartupLocation="CenterOwner">

    <Window.DataContext>
        <ViewModels:StudioViewModel/>
    </Window.DataContext>


    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#3498db" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="🏢 Управление студиями" 
                          Foreground="White" 
                          FontSize="22" 
                          FontWeight="Bold"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnAdd" 
                           Content="➕ Добавить" 
                           Background="#27ae60" 
                           Foreground="White"
                           Padding="12,5"
                           Click="BtnAdd_Click"/>

                    <Button x:Name="btnBack" 
                           Content="← Назад" 
                           Background="#7f8c8d" 
                           Foreground="White"
                           Padding="12,5"
                           Margin="10,0,0,0"
                           Click="BtnBack_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Панель поиска -->
            <Border Grid.Row="0" 
                   Background="#ecf0f1" 
                   CornerRadius="5"
                   Margin="0,0,0,10"
                   Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Поиск:" 
                              Grid.Column="0" 
                              Margin="0,0,10,0"
                              VerticalAlignment="Center"/>

                    <TextBox x:Name="txtSearch" 
                            Grid.Column="1" 
                            MinWidth="200"
                            TextChanged="TxtSearch_TextChanged"/>

                    <Button x:Name="btnClearSearch" 
                           Grid.Column="2" 
                           Content="✕ Очистить" 
                           Margin="10,0,0,0"
                           Padding="8,4"
                           Click="BtnClearSearch_Click"/>
                </Grid>
            </Border>

            <!-- Список студий -->
            <DataGrid Grid.Row="1" 
                     x:Name="dgStudios" 
                     ItemsSource="{Binding Studios}"
                     AutoGenerateColumns="False"
                     SelectionMode="Single"
                     SelectionChanged="DgStudios_SelectionChanged">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Код" 
                                       Binding="{Binding Code}" 
                                       Width="80"/>

                    <DataGridTextColumn Header="Название" 
                                       Binding="{Binding Name}" 
                                       Width="200"/>

                    <DataGridTextColumn Header="Адрес" 
                                       Binding="{Binding Address}" 
                                       Width="200"/>

                    <DataGridTextColumn Header="Стоимость" 
                                       Binding="{Binding RentalCost, StringFormat='{}{0:N0} ₽/час'}" 
                                       Width="120"/>

                    <DataGridTemplateColumn Header="Действия" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️" 
                                           Margin="2"
                                           Padding="8,4"
                                           FontSize="12"
                                           Click="BtnEdit_Click"
                                           Tag="{Binding Id}"
                                           ToolTip="Редактировать"/>

                                    <Button Content="🗑️" 
                                           Margin="2"
                                           Padding="8,4"
                                           FontSize="12"
                                           Click="BtnDelete_Click"
                                           Tag="{Binding Id}"
                                           ToolTip="Удалить"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Статус бар -->
        <StatusBar Grid.Row="2" Background="#ecf0f1">
            <StatusBarItem>
                <TextBlock x:Name="txtStatus" Text="Готово"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding ElementName=dgStudios, Path=Items.Count, StringFormat='Всего студий: {0}'}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
