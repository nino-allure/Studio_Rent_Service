<Window x:Class="Studio_Rent_Service.Views.Clients.ClientEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Studio_Rent_Service.Views.Clients"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="550" 
        Width="500"
        MinHeight="500" 
        MinWidth="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                  Text="{Binding WindowTitle}" 
                  FontSize="20" 
                  FontWeight="Bold"
                  Margin="0,0,0,15"
                  HorizontalAlignment="Center"/>

        <!-- Форма -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Код клиента -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Код клиента:" Grid.Column="0"/>
                    <TextBox Grid.Column="1" 
                            Text="{Binding Code}"
                            IsReadOnly="True"
                            Background="#f5f5f5"/>
                </Grid>

                <!-- Фамилия -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Фамилия:" Grid.Column="0"/>
                    <TextBox Grid.Column="1" 
                            Text="{Binding LastName}"/>
                </Grid>

                <!-- Имя -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Имя:" Grid.Column="0"/>
                    <TextBox Grid.Column="1" 
                            Text="{Binding FirstName}"/>
                </Grid>

                <!-- Отчество -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Отчество:" Grid.Column="0"/>
                    <TextBox Grid.Column="1" 
                            Text="{Binding MiddleName}"/>
                </Grid>

                <!-- Телефон -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Телефон:" Grid.Column="0"/>
                    <TextBox Grid.Column="1" 
                            Text="{Binding Phone}"/>
                </Grid>

                <!-- Email -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Email:" Grid.Column="0"/>
                    <TextBox Grid.Column="1" 
                            Text="{Binding Email}"/>
                </Grid>

                <!-- Дополнительная информация -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Доп. информация:" Grid.Column="0"/>
                    <TextBox Grid.Column="1" 
                            Height="80"
                            Text="{Binding AdditionalInfo}"
                            TextWrapping="Wrap"
                            AcceptsReturn="True"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Center"
                   Margin="0,15,0,0">
            <Button Content="Сохранить" 
                   Background="#27ae60" 
                   Foreground="White"
                   Click="BtnSave_Click"
                   MinWidth="100"/>

            <Button Content="Отмена" 
                   Background="#7f8c8d" 
                   Foreground="White"
                   Click="BtnCancel_Click"
                   Margin="10,0,0,0"
                   MinWidth="100"/>
        </StackPanel>
    </Grid>
</Window>
